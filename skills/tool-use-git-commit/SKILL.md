---
name: tool-use-git-commit
description: Deprecated. Use `tool-use-commit` skill in `skills/commit` as the canonical version.
version: 0.3.0
---

# git-commit

## Status
- 已废弃，不再维护独立流程。
- 请改用 `tool-use-commit`：`skills/tool-use-commit/SKILL.md`。

## Migration
- 旧调用：`git-commit`
- 新调用：`tool-use-commit`

## Backward compatibility
- 历史文档若仍引用 `git-commit`，执行时应按 `tool-use-commit` 技能理解。
